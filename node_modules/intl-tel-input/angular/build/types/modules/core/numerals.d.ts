export declare class Numerals {
    private userNumeralSet?;
    constructor();
    private updateNumeralSet;
    denormalise(str: string, currentInputValue: string): string;
    normalise(str: string): string;
    isAscii(): boolean;
}
